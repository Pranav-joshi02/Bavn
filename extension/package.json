{
  "name": "bavn-extension",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/popup.ts --bundle --outfile=dist/popup.js --platform=browser && esbuild src/contentScript.ts --bundle --outfile=dist/contentScript.js --platform=browser"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.36",
    "esbuild": "^0.27.2",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.91.1"
  }
}
